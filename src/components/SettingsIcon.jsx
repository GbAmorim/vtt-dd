import React from "react";

const SettingsIcon = () => (
    <div className="fixed top-4 right-4 z-50">
        <button className="w-16 h-16 bg-black/50 hover:bg-white/20 rounded-full flex items-center justify-center text-2xl hover:rotate-180 transition-all duration-300 text-gold drop-shadow-lg cursor-pointer">
            ⚙️
        </button>
    </div>
);

export default SettingsIcon;
